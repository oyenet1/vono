# Enforce exact versions — no automatic range resolution
# This means `bun add foo` saves "1.2.3" not "^1.2.3"
save-exact=true

# Prevent accidental publishing of the monorepo root
# (each package publishes individually from packages/*)
